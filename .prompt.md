[
  {
    "task": "Unify AlertDialog Styles",
    "description": "Refactor AlertDialog styles to match the new global dialog style (glassmorphism, padding, radius).",
    "steps": [
      {
        "action": "replace_file_content",
        "file": "frontend/vue-ezchat/src/assets/styles/ez-dialog.css",
        "instructions": [
          "Update `.ez-dialog` class to standard padding (24px side, 20px top?), actually `ez-modern-dialog` used: `border-radius: var(--radius-xl)`, `background: var(--bg-glass)`, `backdrop-filter: var(--blur-glass)`. Check standard usage.",
          "In `GuestAside`, we used `ez-modern-dialog`. I should apply similar properties to `.ez-dialog` in `ez-dialog.css`.",
          "Current `.ez-dialog`: `radius-lg`, `padding: 24px`, `background: var(--bg-card)`. (Not glass?)",
          "Update `.ez-dialog` to use `var(--bg-glass)` and `var(--blur-glass)` (if defined) or copy values from `GuestAside` styles.",
          "Update `.ez-dialog .el-message-box__title` font size/weight to match others.",
          "Update `.ez-dialog .el-message-box__content` styling."
        ]
      }
    ]
  }
]