
# Rename `performJoin` to `executeJoin`

## Goal
Rename the method `performJoin` to `executeJoin` in `useLoginJoin.ts` to better reflect its purpose (executing the join action directly). Update the consumer `JoinChatDialog.vue`.

## Proposed Changes

### 1. Rename Method

#### [MODIFY] [`src/hooks/chat/join/useLoginJoin.ts`](file:///home/dev/EZChat/frontend/vue-ezchat/src/hooks/chat/join/useLoginJoin.ts)
- Rename `performJoin` to `executeJoin`.
- Rename export in return object.

### 2. Update Consumer

#### [MODIFY] [`src/components/dialogs/JoinChatDialog.vue`](file:///home/dev/EZChat/frontend/vue-ezchat/src/components/dialogs/JoinChatDialog.vue)
- Update destructured method name from `performJoin` to `executeJoin`.
- Update usage in `handleJoin`.

## Verification Plan
- `npm run type-check` to ensure no broken references.
